<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.example.todo">

    <application
            android:name=".data.RealmApplication"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">

        <activity
                android:name=".detail.TaskDetailActivity"
                android:label=""
                android:parentActivityName=".list.TaskListActivity"
                android:theme="@style/AppTheme">
        </activity>

        <activity android:name=".list.TaskListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
                android:name=".edit.TaskEditActivity"
                android:label=""
                android:parentActivityName=".list.TaskListActivity"
                android:theme="@style/AppTheme"
                android:launchMode="singleTop"
                android:windowSoftInputMode="adjustResize">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value=".list.TaskListActivity"/>
        </activity>
    </application>

</manifest>